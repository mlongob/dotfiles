- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.gitignore_global: gitignore_global
    ~/.tmux.conf: tmux.conf
    ~/.tmux: tmux/
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.zsh: zsh/
    ~/.zprofile:
        path: zprofile
        force: true
    ~/.zshrc:
        path: zshrc
        force: true
    ~/.config/starship/starship.toml:
        path: config/starship/starship.toml
        create: true
    ~/.config/ghostty/config:
        path: config/ghostty/config
        create: true

- shell:
    - [git submodule update --init --recursive, Updating submodules]
    - command: brew install tmux zsh-autosuggestions zsh-syntax-highlighting fzf zoxide starship && brew install --cask ghostty font-fira-code-nerd-font
      stdin: true
      stdout: true
      stderr: true
    - command: >
        curl -fLo ~/.vim/autoload/plug.vim --create-dirs
        https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
      stdout: true
      stderr: true
      description: Installing vim-plug
    - command: vim +PlugInstall +qall
      stdin: true
      stdout: true
      stderr: true
    - command: >
        tmux new-session -d -s __install__ 2>/dev/null;
        ~/.tmux/plugins/tpm/bin/install_plugins;
        tmux kill-session -t __install__ 2>/dev/null;
        true
      stdout: true
      stderr: true
      description: Installing tmux plugins via TPM
